---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: propeller-ingress
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: propeller-ingress
          servicePort: 80
